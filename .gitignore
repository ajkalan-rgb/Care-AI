# Local Kobo upstream/runtime clones generated by kobo-install
# Do not commit upstream Kobo repositories, generated env files, or Docker runtime output into Care-AI.
upstream/kobo-install/
upstream/kobo-docker/
upstream/kobo-env/

# Local environment files
.env
.env.*
!.env.example

# OS/editor noise
.DS_Store
Thumbs.db
.vscode/
.idea/

# Kobo runtime / cloned upstream folders - never commit
upstream/kobo-install/
upstream/kobo-docker/
upstream/kobo-env/
